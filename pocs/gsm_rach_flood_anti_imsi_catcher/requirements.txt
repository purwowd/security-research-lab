# GSM RACH Flooding PoC - Dependencies
# =====================================

# Core dependencies (required for simulation mode)
# No external dependencies needed for basic simulation

# Optional dependencies for hardware mode
# Install these if you want to use actual SDR hardware:
#
# GNU Radio (install via system package manager):
#   Ubuntu/Debian: sudo apt-get install gnuradio gr-osmosdr python3-gnuradio
#   Arch Linux: sudo pacman -S gnuradio gnuradio-osmosdr
#   Fedora: sudo dnf install gnuradio gr-osmosdr
#
# Or build from source:
#   https://github.com/gnuradio/gnuradio
#   https://github.com/osmocom/gr-osmosdr

# Note: This PoC uses only Python standard library for simulation mode
# No pip packages required for basic functionality
